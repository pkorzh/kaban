import moduleFactory from '../module-factory'

export default moduleFactory('history', {
	mutations: {
		REMOVE(state, payload) {
			// pass
		},
		STAGE(state, payload) {
			// pass
		},
		STAGE_MULTIPLE(state, multiple) {
			// pass
		},
	}
})
