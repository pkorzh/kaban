import '../api/dal/connect';
import { tickets as ticketsDal } from '../api/dal';
import { 
	Ticket,
 } from '../api/dal/models';

async function main() {
}

export default main;

if (require.main === module) {
	main();
}
